'use strict';

module.exports = {
  extends: ['plugin:jsdoc/recommended'],
  plugins: ['jsdoc'],
  overrides: [
    {
      files: ['*'],
      extends: ['plugin:compat/recommended'],
      plugins: ['compat'],
    },
  ],
};
