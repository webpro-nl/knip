const noop = () => void 0;
export {
  noop as bench,
  noop as createExpect,
  noop as describe,
  noop as expect,
  noop as it,
  noop as suite,
  noop as test,
};
export { noop as assert, noop as should };
