name: CI

on: push

jobs:
  stylelint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Run stylelint check
        run: yarn stylelint
        working-directory: packages/app
      - name: Generate icons
        run: yarn icons
        working-directory: packages/app
