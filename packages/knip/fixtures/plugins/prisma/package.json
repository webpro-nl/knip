{
  "name": "@fixtures/prisma",
  "version": "*",
  "devDependencies": {
    "prisma": "*"
  },
  "scripts": {
    "generate": "prisma generate",
    "generate-with-config": "prisma generate --config prisma.config.ts"
  }
}
