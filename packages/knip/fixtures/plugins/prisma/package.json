{
  "name": "@fixtures/prisma",
  "devDependencies": {
    "prisma": "*",
    "tsx": "*"
  },
  "scripts": {
    "generate": "prisma generate --config config/prisma.custom-config.ts"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
