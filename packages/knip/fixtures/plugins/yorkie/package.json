{
  "name": "@fixtures/yorkie",
  "version": "*",
  "gitHooks": {
    "pre-commit": "lint-staged"
  },
  "lint-staged": {
    "*.js": "eslint --fix",
    "*.md": "markdownlint --fix",
    "docs/**/*.svg": [
      "npx svgo -r --multipass"
    ]
  },
  "devDependencies": {
    "lint-staged": "^11.0.0",
    "yorkie": "^2.0.0"
  }
}
