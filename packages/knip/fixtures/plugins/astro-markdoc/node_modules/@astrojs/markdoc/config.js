export function defineMarkdocConfig(config) {
  return config;
}

export function component(path, namedExport) {
  return namedExport ? `${path}#${namedExport}` : path;
}

export const nodes = {
  heading: {}
};
