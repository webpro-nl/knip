export enum NamedKey {
  used = 1,
  unusedFirst = 2,
  unusedSecond = 3,
}

export enum NumericKey {
  normal = 1000,
  info = 2000,
  warning = 3000,
}

export enum NamespaceNamedKey {
  used = 1,
  unused = 2,
}

export enum NumberLikeKey {
  Infinity = 1,
  '0x10' = 2,
  '1e3' = 3,
  unused = 4,
}
